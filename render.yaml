services:
  - type: web
    name: inventory-tracker
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: DATABASE_URL
        value: mysql://root:JacksonYee1128@db.render.com:3306/inventory_db
